---
description: "Safe git undo by phase/plan manifest — reverts only (cairn — /cairn:help for the verb reference)"
argument-hint: "<phase|plan> | --apply"
---

Execute the cairn verb `backtrack`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/backtrack.md` and execute it with
   `$ARGUMENTS` as its arguments.
