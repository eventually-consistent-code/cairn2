---
description: "Ship-time synthesis — plans + cards → public-safe docs/ (cairn — /cairn:help for the verb reference)"
---

Execute the cairn verb `distill`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/distill.md` and execute it with
   `$ARGUMENTS` as its arguments.
