---
description: "Open-issue sweep against project conventions — report by default, --apply for the safe subset (cairn — /cairn:help for the verb reference)"
argument-hint: "[--stale-days N] | --apply"
---

Execute the cairn verb `triage`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/triage.md` and execute it with
   `$ARGUMENTS` as its arguments.
