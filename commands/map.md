---
description: "Project knowledge graph — build, query, diff, status (cairn — /cairn:help for the verb reference)"
argument-hint: "build | \"<question>\" | diff | status"
---

Execute the cairn verb `map`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/map.md` and execute it with
   `$ARGUMENTS` as its arguments.
