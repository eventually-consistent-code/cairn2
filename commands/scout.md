---
description: "Research a phase only — resumable RESEARCH.md checkpoints (cairn — /cairn:help for the verb reference)"
argument-hint: "<N>"
---

Execute the cairn verb `scout`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/scout.md` and execute it with
   `$ARGUMENTS` as its arguments.
