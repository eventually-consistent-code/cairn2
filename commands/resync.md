---
description: "Detect out-of-band commits; refresh plan context (cairn — /cairn:help for the verb reference)"
---

Execute the cairn verb `resync`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/resync.md` and execute it with
   `$ARGUMENTS` as its arguments.
