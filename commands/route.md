---
description: "Re-route the roadmap — insert/remove/edit phases (cairn — /cairn:help for the verb reference)"
argument-hint: "insert|remove|edit <N> [\"name\"]"
---

Execute the cairn verb `route`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/route.md` and execute it with
   `$ARGUMENTS` as its arguments.
