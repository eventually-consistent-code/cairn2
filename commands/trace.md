---
description: "Persistent debugging session — evidence→hypothesis→test, tracker-mirrored (cairn — /cairn:help for the verb reference)"
argument-hint: "[\"<bug>\" | <id> | close <id>]"
---

Execute the cairn verb `trace`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/trace.md` and execute it with
   `$ARGUMENTS` as its arguments.
