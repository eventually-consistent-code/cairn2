---
description: "Planning-dir health, repair, and workflow forensics (cairn — /cairn:help for the verb reference)"
argument-hint: "[--repair] | forensics [phase]"
---

Execute the cairn verb `medic`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/medic.md` and execute it with
   `$ARGUMENTS` as its arguments.
