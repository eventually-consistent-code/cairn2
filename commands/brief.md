---
description: "Onboarding briefing from cards + plans (cairn — /cairn:help for the verb reference)"
argument-hint: "[--stdout]"
---

Execute the cairn verb `brief`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/brief.md` and execute it with
   `$ARGUMENTS` as its arguments.
