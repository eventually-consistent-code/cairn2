---
description: "Pause/resume session continuity (cairn — /cairn:help for the verb reference)"
argument-hint: "[resume]"
---

Execute the cairn verb `waypoint`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/waypoint.md` and execute it with
   `$ARGUMENTS` as its arguments.
