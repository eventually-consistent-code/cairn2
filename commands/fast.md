---
description: "Trivial inline change — one issue, ≤3 files, atomic commit (cairn — /cairn:help for the verb reference)"
argument-hint: "\"<change>\""
---

Execute the cairn verb `fast`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/fast.md` and execute it with
   `$ARGUMENTS` as its arguments.
