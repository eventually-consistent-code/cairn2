---
description: "Publish repo docs to the configured docs connector (Confluence) — landing page + page tree (cairn — /cairn:help for the verb reference)"
argument-hint: "publish [--name \"<project>\"] | (none = status)"
---

Execute the cairn verb `docs`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/docs.md` and execute it with
   `$ARGUMENTS` as its arguments.
