---
description: "Configure cairn.json — models, continuity, leak guard (cairn — /cairn:help for the verb reference)"
argument-hint: "[key] [value]"
---

Execute the cairn verb `tune`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/tune.md` and execute it with
   `$ARGUMENTS` as its arguments.
