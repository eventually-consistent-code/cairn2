---
description: "Portfolio board — every cairn project on this machine, staleness-flagged (cairn — /cairn:help for the verb reference)"
argument-hint: "[<project>]"
---

Execute the cairn verb `outlook`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/outlook.md` and execute it with
   `$ARGUMENTS` as its arguments.
