---
description: "Render this verb reference (cairn — /cairn:help for the verb reference)"
argument-hint: "[verb]"
---

Execute the cairn verb `help`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/help.md` and execute it with
   `$ARGUMENTS` as its arguments.
