---
description: "Search memory, scoped to the active context, with staleness flags (cairn — /cairn:help for the verb reference)"
argument-hint: "\"<query>\" [--phase N] [--issue <id>]"
---

Execute the cairn verb `recall`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/recall.md` and execute it with
   `$ARGUMENTS` as its arguments.
