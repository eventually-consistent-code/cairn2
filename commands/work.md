---
description: "Execute a phase — claim issues, do the work, close on verified done (cairn — /cairn:help for the verb reference)"
argument-hint: "<N> [--wave [N]]"
---

Execute the cairn verb `work`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/work.md` and execute it with
   `$ARGUMENTS` as its arguments.
