---
description: "Goal-backward phase check, drift clean, write VERIFICATION.md (cairn — /cairn:help for the verb reference)"
argument-hint: "<N>"
---

Execute the cairn verb `verify`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/verify.md` and execute it with
   `$ARGUMENTS` as its arguments.
