---
description: "Multi-project workspaces — dispatch board, focus, parallel workstreams (cairn — /cairn:help for the verb reference)"
argument-hint: "init | focus <member> | dispatch | claim|update|done <id>"
---

Execute the cairn verb `basecamp`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/basecamp.md` and execute it with
   `$ARGUMENTS` as its arguments.
