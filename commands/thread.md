---
description: "Persistent context threads that survive /clear (cairn — /cairn:help for the verb reference)"
argument-hint: "\"<name>\" | (none = list open) | --wrap"
---

Execute the cairn verb `thread`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/thread.md` and execute it with
   `$ARGUMENTS` as its arguments.
