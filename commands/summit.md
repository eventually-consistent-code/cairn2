---
description: "Complete the milestone — verify gate, tracker close/release, archive, tag (cairn — /cairn:help for the verb reference)"
---

Execute the cairn verb `summit`:

1. Read `skills/cairn-trailhead/SKILL.md` (this plugin) — its shared
   rules apply to every step below.
2. Read `skills/cairn-trailhead/verbs/summit.md` and execute it with
   `$ARGUMENTS` as its arguments.
