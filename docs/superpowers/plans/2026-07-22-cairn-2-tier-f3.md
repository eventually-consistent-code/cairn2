# Cairn 2.0 — Tier F3: Frontend Quality Loop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Full #2290 on existing rails — Designer + UAT agents, tokens.json discipline, map-edge traceability, fidelity contract. Zero server change. Spec: `docs/superpowers/specs/2026-07-22-cairn-2-tier-f3-frontend-loop.md`.

**Architecture:** One PR, one stage, two tasks: agents + verb wiring, then docs + verification.

## Global Constraints

- Base branch: `main`. Gate every commit: `node scripts/check-surface.mjs && cd server && npx vitest run && npx tsc --noEmit` — all UNTOUCHED-green (36 live / 0 reserved / 62 tools; `git status server/` clean; zero server diffs the whole tier).
- Agent files at EXACTLY `agents/cairn-designer.md`, `agents/cairn-uat.md` with plugin-agent frontmatter (`name`, `description` incl. when-to-use, `tools:` allow-list). Tool lists EXACTLY per spec §2/§3.
- Tokens file convention EXACTLY `.cairn/draft/themes/tokens.json`; the both-files rule (CSS + tokens in every token change) stated wherever tokens appear.
- Traceability edges EXACTLY: requirement `issue` node —`implements`→ `decision` node (map's existing enums; no new types).
- Verb-doc edits are ADDITIVE sections/sentences — no rewrites of shipped discipline.

## File Structure (end state)

```
agents/cairn-designer.md agents/cairn-uat.md   # new
skills/cairn-trailhead/verbs/{draft,audit,map}.md   # additive wiring
README.md VERIFICATION.md                       # tier record
```

### Task 1: The two agents + verb wiring

**Files:** Create both agent files; modify the three verb docs.

- [ ] **Step 1: Read the rails first** — verbs/draft.md, audit.md, map.md end-to-end; an existing plugin agent example for frontmatter shape (any `agents/*.md` from an installed plugin, e.g. under ~/.claude/plugins/cache — read ONE for format only).
- [ ] **Step 2: `agents/cairn-designer.md`** (~80-100 lines) — spec §2 in full: role statement; the three-stage flow (wireframes low-fi → tokens BOTH-files → coded prototypes on tokens), each stage's pick = draft `decision` entry + mirror comment; `map_set` decision nodes + `implements` edges from the requirement issue as directions lock; hard rules verbatim (tools only, never raw file edits to map/board; tokens+CSS never diverge; leak rules; throwaway until --wrap). Frontmatter tools EXACTLY: draft_log, issue_comment, map_set, map_get, session_landscape (plus Read/Write/Edit/Glob/Grep for the artifact files) — draft_start/draft_close removed (amended 2026-07-22 with the spec — lifecycle belongs to the verb).
- [ ] **Step 3: `agents/cairn-uat.md`** (~80-100 lines) — spec §3 in full: requirement enumeration (issue_list + map edges); platform-matrix walks (name what was exercised, desktop+mobile viewport minimum); evidence+verdict per flow; the traceability sweep (untraced requirement = important finding); fidelity handoff (divergence = audit ui finding citing the violated decision entry); audit closing discipline + --fix contract. Frontmatter tools EXACTLY: issue_list, issue_get, issue_create, issue_comment, issue_close, map_get, audit_record, trace_start, plan_status (plus Read/Glob/Grep/Bash for driving the app).
- [ ] **Step 4: Verb wiring (additive)** — draft.md "Designer dispatch" section + tokens.json discipline; audit.md ui row + fidelity sentence, uat row + platform/traceability sentences, both naming cairn-uat as the specialist; map.md one build line (requirement→decision implements edges). check-surface must stay green (agents aren't ratcheted; verb tool refs unchanged).
- [ ] **Step 5: Gate + commit** — `feat(plugin): designer + uat agents — the #2290 loop on existing rails`

### Task 2: Docs + verification

**Files:** `README.md`, `VERIFICATION.md`.

- [ ] **Step 1: README** — Agents section (two rows: name, role, dispatched-by); Tier F3 shipped; Tier F row → complete.
- [ ] **Step 2: VERIFICATION.md** — Tier F3 section, house format: the zero-server-change statement with its evidence command (`git diff main -- server/` empty), agent frontmatter/tool-list verification notes (every tool grepped against the registry — do it and cite), criteria 1-6 mapped, `drill-frontend-loop.mjs` procedure PENDING itemized per spec §5.
- [ ] **Step 3: Gate + commit** — `docs(cairn): Tier F3 verification record — drill procedure pending live run`

---

## Post-merge (tier convention)

PR → merge → author + run `server/drills/drill-frontend-loop.mjs` → commit the drills-run record.
