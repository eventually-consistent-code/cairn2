# Cairn 2.0 — Tier 0: Trailhead Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** One `/cairn <verb>` entrypoint replaces the seven per-verb command files. A `cairn-trailhead` routing skill holds the canonical verb table (11 live verbs, 17 reserved with tier assignments) and per-verb subroutine files loaded only on invocation; old `/cairn:<verb>` forms survive as two-line shims until P5′. Ships cairn2's own plugin manifest so the surface is dogfood-installable, and a surface-conformance check that hard-fails on routing-table/subroutine/tool-reference drift. Spec: `docs/superpowers/specs/2026-07-15-cairn-2-tier-0-trailhead.md`.

**Architecture:** Pure plugin-layer restructure — zero server changes. `commands/cairn.md` parses the verb from `$ARGUMENTS` and delegates to `skills/cairn-trailhead/SKILL.md`, whose routing table maps each verb to `skills/cairn-trailhead/verbs/<verb>.md`. Existing command bodies move into subroutines; shared boilerplate (error posture, precedence rule, batch-questions rule) hoists into SKILL.md and is stated once. `cairn-planning`/`cairn-memory` skills are untouched — they own policy, subroutines own procedure. The conformance check is a Node script (`scripts/check-surface.mjs`) run by `npm test` in CI: it parses the routing table, verifies live-verb ↔ subroutine-file bijection, and validates every `` `tool_name` `` reference in verb docs against the names extracted from `server/src/index.ts` `registerTool("…")` calls.

**Tech Stack:** Markdown (commands/skills), Node ≥ 20 ESM for the check script (no new dependencies). Server untouched.

## Global Constraints

- Base branch: `main` (includes P0–P4 + recovered command surface at `commands/`). All paths repo-relative.
- **Zero server changes.** `server/` is not touched by any task; the 23-tool registry is read-only input to the check script.
- Live verbs EXACTLY: `plan work verify ship status new import remember recall help do` (11). Reserved verbs EXACTLY as the spec's table: `waypoint`(A0) `scout probe draft route summit auto fast resync`(A) `mark retro distill brief tune`(B) `trace`(C) `triage`(D) `basecamp`(F) (16).
- Routing-table row format EXACTLY: `| verb | purpose (one line) | args | verbs/<verb>.md | live\|reserved-<tier> |`. The table is the single registry — `help` renders from it, the check script parses it.
- Subroutine frontmatter EXACTLY three fields: `verb` (string), `args` (string, the argument-hint), `status` (`live`). Reserved verbs get table rows only — creating a `verbs/*.md` for a reserved verb is a conformance failure.
- Shim content EXACTLY (per old command file, keeping its original `description` frontmatter): body replaced by `Moved: run \`/cairn <verb> $ARGUMENTS\` — then execute skills/cairn-trailhead/verbs/<verb>.md with those arguments.` Shims must still work, not just redirect text.
- Migration is move-and-dedupe, NOT rewrite: subroutine procedure steps stay semantically identical to the P4-final command bodies; only boilerplate duplicated across files may hoist to SKILL.md. Tool names in verb docs must match the server registry exactly (`context_get context_set issue_create issue_get issue_update issue_close issue_list plan_scaffold_project plan_scaffold_phase plan_status plan_phase_ensure plan_drift plan_issues_set plan_import plan_unplanned mem_index mem_search mem_stats mem_card_create mem_card_list mem_card_recall` — 21 of 23; `remember`/`recall` subroutines wrap the `mem_*` set per the cairn-memory skill).
- `do` never mutates tracker state without confirmation; unknown verbs route to `help` with the token echoed and a nearest-verb suggestion — never fall through to `do`.
- Conventional commits; each task commits atomically; `cd server && npx vitest run && npx tsc --noEmit` green before every commit (proves server untouched); `node scripts/check-surface.mjs` green from Task 4 onward.

## File Structure (end state)

```
.claude-plugin/plugin.json          # new — cairn2 dogfood manifest
commands/
  cairn.md                          # new — the one entrypoint
  new.md plan.md work.md verify.md ship.md status.md import.md   # shims
skills/cairn-trailhead/
  SKILL.md                          # routing table + shared rules
  verbs/
    plan.md work.md verify.md ship.md status.md new.md import.md
    remember.md recall.md help.md do.md
scripts/check-surface.mjs           # conformance check
docs/superpowers/plans/2026-07-15-cairn-2-tier-0.md   # this plan
VERIFICATION.md additions happen at execute-time (footprint numbers)
```

---

### Task 1: Plugin manifest + entrypoint command

**Files:**
- Create: `.claude-plugin/plugin.json`
- Create: `commands/cairn.md`

**Interfaces:**
- Produces: installable dogfood plugin `cairn2`; `/cairn2:cairn <verb> [args]` (namespaced by plugin name; referred to as `/cairn` throughout).

**Steps:**

- [ ] **Step 1: Manifest.** `plugin.json`: name `cairn2`, version `2.0.0-alpha.0`, description "cairn 2.0 (private dogfood build) — converged work management", author/homepage/repository pointing at `eventually-consistent-code/cairn2`, license MIT, no dependencies (2.0 is self-contained — do NOT declare gsd/context-mode). Verify `.mcp.json` launches the server via a path valid from the plugin root.
- [ ] **Step 2: Entrypoint.** `commands/cairn.md` frontmatter: `description: "Cairn — one entrypoint, every verb. Run /cairn help for the verb reference."`, `argument-hint: "<verb> [args]"`. Body: split `$ARGUMENTS` into first token (verb) and remainder; no verb → treat as `help`; then: read `skills/cairn-trailhead/SKILL.md`, look the verb up in the routing table, read and execute its subroutine with the remainder as arguments; unknown verb → execute `verbs/help.md` passing the unrecognized token.
- [ ] **Step 3: Verify.** `claude plugin validate .` (or manual load) accepts the manifest; `cd server && npx vitest run && npx tsc --noEmit` green. Commit `feat(plugin): cairn2 manifest + /cairn entrypoint`.

### Task 2: cairn-trailhead SKILL.md (routing table + shared rules)

**Files:**
- Create: `skills/cairn-trailhead/SKILL.md`

**Interfaces:**
- Produces: the canonical routing table (11 live + 17 reserved rows, format per Global Constraints); shared-rules section.
- Consumes: verb vocabulary from the Tier 0 spec (locked).

**Steps:**

- [ ] **Step 1:** Frontmatter `name: cairn-trailhead`, description: "Use when routing /cairn <verb> invocations — owns the verb registry and shared execution rules for every cairn subroutine."
- [ ] **Step 2:** Routing table with all 28 rows. Purpose column one line each; reserved rows carry `reserved-<tier>` status and no subroutine path is created for them (path column still names the future file).
- [ ] **Step 3:** Shared rules section, stated once, inherited by all subroutines: typed-error posture (surface the server's error code + next action, never a stack trace), precedence rule (git plan docs win over tracker text — update the issue, never follow it), batch-questions rule (related questions in one AskUserQuestion, never one-at-a-time), active-context awareness (verbs operate on the active project/phase/issue unless args override).
- [ ] **Step 4:** Commit `feat(plugin): cairn-trailhead routing skill`.

### Task 3: Verb subroutines (migrate 7, author 4)

**Files:**
- Create: `skills/cairn-trailhead/verbs/{plan,work,verify,ship,status,new,import}.md` (moved bodies)
- Create: `skills/cairn-trailhead/verbs/{remember,recall,help,do}.md` (new)

**Interfaces:**
- Consumes: P4-final command bodies in `commands/*.md`; `cairn-memory` skill (remember/recall policy); routing table (help/do).
- Produces: 11 subroutines, each with the three-field frontmatter and a "next step" closing line.

**Steps:**

- [ ] **Step 1: Migrate seven.** For each of plan/work/verify/ship/status/new/import: copy the command body verbatim into the subroutine under the frontmatter (`verb`, `args` = old `argument-hint`, `status: live`); delete boilerplate now covered by SKILL.md shared rules; keep every procedure step and tool call unchanged.
- [ ] **Step 2: remember.md / recall.md.** Thin procedure wrappers: `remember` → per cairn-memory skill judgment, `mem_card_create` (durable fact) or `mem_index` (bulk); `recall` → `mem_search`/`mem_card_recall` scoped by active-context, surfacing `STALE` flags per the skill's re-verify policy. Policy stays in `skills/cairn-memory/SKILL.md`; these files only sequence the calls.
- [ ] **Step 3: help.md.** Render the routing table: live verbs grouped by lifecycle order with purpose + args; reserved verbs listed compactly by tier ("coming in Tier A: scout, probe, …"). Close with `/cairn do "<what you want>"` hint.
- [ ] **Step 4: do.md.** Classify freeform intent against the routing table's purpose column; single clear match on a read-only verb → run it; mutating verb or low confidence → confirm ("Sounds like `/cairn plan 4 --deep` — run it?") before executing; no plausible match → help.
- [ ] **Step 5:** Commit `feat(plugin): verb subroutines — 7 migrated, remember/recall/help/do authored`.

### Task 4: Shims + conformance check

**Files:**
- Modify: `commands/{new,plan,work,verify,ship,status,import}.md` → shims
- Create: `scripts/check-surface.mjs`
- Modify: `server/package.json` — NO. Check runs standalone: add root-level invocation to CI in Task 5 instead.

**Interfaces:**
- Produces: `check-surface.mjs` exit 0/1; parses SKILL.md table rows `/^\|\s*`?([a-z]+)`?\s*\|/`, extracts registry via `/registerTool\("([a-z_]+)"/g` from `server/src/index.ts`, extracts tool refs from verb docs via `` /`([a-z]+_[a-z_]+)`/g `` filtered to known prefixes (`context_|issue_|plan_|mem_`).

**Steps:**

- [ ] **Step 1: Shims.** Replace each old command body per the Global Constraints shim format; keep original frontmatter `description` prefixed with "(moved) ".
- [ ] **Step 2: check-surface.mjs.** Checks, each with a named failure message: (a) every `live` table row has its `verbs/<verb>.md`; (b) every `verbs/*.md` file has a `live` table row (reserved-with-file = failure); (c) subroutine frontmatter has exactly `verb`/`args`/`status` and `verb` matches filename; (d) every prefixed tool reference in `verbs/*.md` exists in the server registry; (e) reserved verb set matches the spec list exactly.
- [ ] **Step 3:** Run `node scripts/check-surface.mjs` — green. Seed one deliberate breakage of each class locally to confirm detection (not committed). Commit `feat(plugin): command shims + surface-conformance check`.

### Task 5: CI wiring + dogfood verification

**Files:**
- Create: `.github/workflows/ci.yml`
- Create/Modify: `VERIFICATION.md` (repo root)

**Interfaces:**
- Produces: CI running server tests + tsc + surface check on push/PR; recorded token-footprint numbers and lifecycle-drill results.

**Steps:**

- [ ] **Step 1: ci.yml.** Node 20; jobs: `cd server && npm ci && npx vitest run --exclude '**/*.live.test.ts' && npx tsc --noEmit`, then `node scripts/check-surface.mjs` at root.
- [ ] **Step 2: Footprint measurement.** Record always-present cost before (7 command descriptions) vs after (1 entrypoint + 7 two-line shims): count tokens of the registered command frontmatter/description text (estimate `chars/4`). Write both numbers + method into `VERIFICATION.md`.
- [ ] **Step 3: Dogfood drill.** Install cairn2 as a local plugin; against a scratch project + real tracker: `/cairn new` → `/cairn plan 1` → `/cairn work 1` → `/cairn verify 1` → `/cairn ship`, plus `/cairn do "what's the status"` (routes to status, read-only, no confirm needed → runs) and `/cairn wrok` (typo → help with "did you mean work?"). Record results in `VERIFICATION.md`.
- [ ] **Step 4:** Commit `ci: server tests + surface conformance; verification record`. Push.

---

**Success criteria traceability:** spec criteria 1–2 → Task 5 drill; 3 → Task 4 shims; 4 → Task 3 do/help + Task 5 drill; 5 → Task 5 footprint; 6 → Task 4 check + Task 5 CI.
